using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateAudioclip : MonoBehaviour
{
    /*

    public DontDestroyOnLoadMusic the_holder;

    public AudioClip working_on;

    // Start is called before the first frame update
    void Start()
    {
        if (Mind.made_sound)
        {
            Destroy(gameObject);
        } else
        {
            the_holder.VHS_exception = working_on;
            Mind.made_sound = true;
        }
    }

    */
}
